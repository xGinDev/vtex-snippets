{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "The title of the block see in Site Editor"
    },
    "props": {
      "type": "object",
      "description": "The props of the block",
      "properties": {
        "textAlign": {
          "type": "string",
          "description": "Product price position on the list.",
          "default": "left"
        },
        "showListPrice": {
          "type": "boolean",
          "description": "Whether the product prices should be displayed on the list (true) or not (false).",
          "default": true
        }
      }
    }
  },
  "required": ["title"]
}
