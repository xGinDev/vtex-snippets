{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "description": "The title of the block see in Site Editor"
    },
    "props": {
      "type": "object",
      "description": "The props of the block",
      "properties": {
        "itemCountMode": {
          "anyOf": [
            {
              "type": "string",
              "enum": ["rows", "cols"]
            }
          ],
          "description": "Availability message position on the list. Possible values are: rows (displaying the message in the product row) or cols (displaying in the product column).",
          "default": "cols"
        }
      }
    }
  },
  "required": ["title"]
}
